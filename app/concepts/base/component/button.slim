- if @path
  = tag.a href: @path, **html_options do
    - if @text
      = @text
    - else
      i.bi class="#{icon_class} fs-5"
- else
  = tag.button **html_options do
    - if @text
      = @text
    - else
      i.bi class="#{icon_class} fs-5"
